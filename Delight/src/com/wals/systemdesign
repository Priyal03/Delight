ride later feature

first request prior to 1 hours and from 24 hours.

AvailableToBook flag > timings > location. in drivers how much filter?

send the request to this drivers list

whoever accepts.. we get some drivers.. we'll only accept till first 10 drivers. 

first driver. or based on the rating

send response to the customer.


binary tree to doubly linked list

root, and left is leaf node then interchange the links of it.

then do the same for right as well.

node.left & node.right == null 